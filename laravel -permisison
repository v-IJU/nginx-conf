cd /path/to/your/laravel/project

# Set correct ownership
sudo chown -R www-data:www-data storage bootstrap/cache

# Set directory permissions
sudo find storage bootstrap/cache -type d -exec chmod 775 {} \;

# Set file permissions
sudo find storage bootstrap/cache -type f -exec chmod 664 {} \;

sudo chmod -R 775 bootstrap/cache

For webbazaar server
==============================
sudo chown -R wpdev:wpdev storage bootstrap/cache
sudo find storage -type d -exec chmod 775 {} \;
sudo find storage -type f -exec chmod 664 {} \;
sudo chmod -R 775 bootstrap/cache


#for log permisison issues 
sudo chown -R www-data:www-data storage bootstrap/cache
sudo chmod -R 775 storage bootstrap/cache
'daily' => [
    'driver' => 'daily',
    'path' => storage_path('logs/laravel.log'),
    'level' => 'debug',
    'days' => 14,
    'permission' => 0664,  // <--- add this line
],

#after storage link unisharp filemanager if images showing 404

sudo chown -h wbdev:wbdev public/storage
